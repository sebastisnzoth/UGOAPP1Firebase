rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default Access: Allow anyone read/write for development. 
    // In production, restrict this.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
